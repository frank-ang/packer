version: 2.1
orbs:
  aws-cli: circleci/aws-cli@3.1.1
workflows:
  build_and_test:
    jobs:
      - test
jobs:
  test:
    docker:
      - image: cimg/python:3.10.4-node
    steps:
      - checkout
      - run:
          name: Dump environment
          command: |
            printenv
            pwd
            df -h
            mount
            lsblk
            cat /proc/meminfo
      - run:
          name: Install dependencies 
          command: |
            echo "## Installing Dependencies..."
            sudo apt-get update -y
            sudo apt-get install -y rsync
            sudo npm install -g ipfs-car
            pip install -r requirements.txt
      - run:
          name: Init Test Data
          command: |
            make init_testdata || true
      - run:
          name: Run tests
          command: |
            echo "## Running regular tests..."
            make test_all
            python -m pytest test/test_packer.py -o log_cli=true -o log_cli_level=INFO --junitxml=reports/test-report.xml
## Fails due to limited 100GB disk size on the CircleCI machine.
# Probably need to run larger load tests outside of CI.
#      - run:
#          name: Run large test
#          command: |
#            echo "## Running large tests..."
#            make init_largedata
#            no_output_timeout: 30m
#            make test_large
#            no_output_timeout: 30m
      - store_test_results:
          path: reports/
      - store_artifacts:
          path: reports/test-report.xml
          destination: pytest-report.xml
      - store_artifacts:
          path: reports/
